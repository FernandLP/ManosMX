<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal para Modo Oscuro -->
    <style name="Theme.Lenguajedesenias" parent="Theme.MaterialComponents.DayNight">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorSecondary">@color/secondary_dark</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>

        <!-- Colores de fondo y superficie -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">@color/background_dark</item>
        <item name="colorOnBackground">@color/on_dark_background</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>

        <!-- Color del texto -->
        <item name="android:textColorPrimary">@color/text_primary_dark</item>
        <item name="android:textColorSecondary">@color/secondary_dark</item>

        <!-- Color del acento -->
        <item name="colorAccent">@color/accent_dark</item>

        <!-- Colores para botones -->
        <item name="android:buttonStyle">@style/Widget.MaterialComponents.Button</item>
        <item name="colorButtonNormal">@color/primary_dark</item>
<!--        <item name="android:textColorButton">@color/text_primary_dark</item>-->

        <!-- Colores para manejo de errores y éxito -->
        <item name="colorError">@color/error_dark</item>
        <item name="colorOnError">@color/text_primary_dark</item>
<!--        <item name="colorSuccess">@color/success_dark</item>-->
    </style>
</resources>
