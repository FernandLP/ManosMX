<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal para Modo Claro -->
    <style name="Theme.Lenguajedesenias" parent="Theme.MaterialComponents.DayNight">
        <!-- Colores principales -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryVariant">@color/primary_light</item>
        <item name="colorSecondary">@color/secondary_light</item>
        <item name="colorSecondaryVariant">@color/secondary_light</item>

        <!-- Colores de fondo y superficie -->
        <item name="android:colorBackground">@color/background_light</item>
        <item name="colorSurface">@color/background_light</item>
        <item name="colorOnBackground">@color/on_light_background</item>
        <item name="colorOnSurface">@color/text_primary_light</item>

        <!-- Color del texto -->
        <item name="android:textColorPrimary">@color/text_primary_light</item>
        <item name="android:textColorSecondary">@color/secondary_light</item>

        <!-- Color del acento -->
        <item name="colorAccent">@color/accent_light</item>

        <!-- Colores para botones -->
        <item name="android:buttonStyle">@style/Widget.MaterialComponents.Button</item>
        <item name="colorButtonNormal">@color/primary_light</item>
<!--        <item name="android:textColorButton">@color/text_primary_light</item>-->

        <!-- Colores para manejo de errores y éxito -->
        <item name="colorError">@color/error_light</item>
        <item name="colorOnError">@color/text_primary_light</item>
<!--        <item name="colorSuccess">@color/success_light</item>-->
    </style>
</resources>
